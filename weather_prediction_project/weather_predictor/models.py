from django.db import models

# This weather prediction project uses ML models instead of Django models
# All data persistence is handled through the ML model files and CSV data

# If you want to add user management, prediction history, etc.,
# you can add Django models here in the future
